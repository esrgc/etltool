@model IEnumerable<IDictionary<string, object>>
@{
    ViewBag.Title = "Data Preview";
}
@section header{
    <h3>@ViewBag.Title</h3>
    <ul class="breadcrumb">
        <li><a href="@Url.Action("index", "Home")">Home</a><span class="divider">/</span></li>
        <li><a href="@Url.Action("index", "Upload")">Upload</a><span class="divider">/</span></li>
        <li class="active">@ViewBag.Title</li>
    </ul>
}
<!--Content-->
<div class="row-fluid">
    <div class="well span12">
        <h4>Instructions</h4>
        <ol>
            <li>
                Instruction for this step ---> preview data asldkfj lfkjw rwoiyr aoihovnda nfeoina
                cvoihwnea oiadhf eoifha oh doaihefoaneo ahfuhebiwb abfiub v,wehoih avoeih this veiand
                oeijar dkvha kiaer vkaheroi ivhoihc eihfhia fei f ivenvieoahdh feov
            </li>
            <li>
                baldef daief lakhfi alkfeoih adkhf lalifne 
            </li>
        </ol>
    </div>
</div>
<div class="row-fluid">
    <div class="span12">
        <h4>Uploaded data</h4>
        <table class="table table-hover">
            <thead>
                <tr>
                    @foreach (var key in Model.First().Keys) { 
                        <th>@key</th>
                    }
                </tr>
            </thead>
            <tbody>
                @foreach (var row in Model) { 
                    <tr>
                        @foreach (var key in row.Keys) { 
                            <td>@row[key]</td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div class="row-fluid">
    <div class="span12 form-actions">
        <a href="@Url.Content("~/Upload/index")" class="btn">Back</a>
        <a href="@Url.Content("~/Upload/MapData")" class="btn btn-primary">Next</a>
    </div>
</div>


